import React from 'react';

// This component is not currently used as comment functionality has been removed.
// It's kept as a placeholder for potential future static comment systems.

// Define an empty interface for props if no props are expected, or define them if needed.
// eslint-disable-next-line @typescript-eslint/no-empty-interface
interface CommentProps {}

const Comment: React.FC<CommentProps> = () => {
  return null; // Or a placeholder message if preferred
};

export default Comment;